# Pose classifier models

Copy these files from the existing Pose_actionrecognition project:

- `models/classifier/model_weights.xgb`
- `models/classifier/classes.json`

`classes.json` must preserve the training class order. The expected labels are
`squat`, `run`, `sit`, `stretch`, `walk`, `jump`, `bendover`, `stand`, and
`lying`.

